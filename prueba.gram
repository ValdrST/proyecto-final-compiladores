func ent main(sin) 
inicio
	devolver 1
fin

ent var_1,var_2
real rvar_1
dreal drvar_1
car letra

func ent sum(ent x, ent y)
inicio
	registro
	inicio
		ent r1
		ent r2
	fin reg
	ent z
	z := x + y
	devolver z
fin

func sin main2()
inicio
	devolver
fin